# ATENÇÃO: altere o formato de terminação de linha de CRLF para LF no VSCode ou outro editor.

# Define explicitamente que todos os comandos devem ser executados com o shell bash
SHELL=/bin/bash

# Executa a verificação de saúde a cada 5 minutos
*/5 * * * * /app/check_docker_health.sh >> /var/log/cron.log 2>&1

# Executa o script de backup todos os dias à 1AM
58 23 * * * /app/backup_wrapper.sh >> /var/log/cron.log 2>&1

# Linha em branco no final